# Notas:

Este es un programa en node

```

Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de a tabla de multitiplicar     [número] [requerido]
  -l, --listar   Muestra o lista la tabla de multiplicar realizada
                                                     [booleano] [defecto: false]
  -h, --hasta    Esel numero que define hasta que numero debe continuar la tabla
                                                          [número] [defecto: 10]

```